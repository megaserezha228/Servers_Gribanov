# KR2
# Задание 3.1
POST /create_user - создание пользователя с валидацией данных

# Задание 3.2
GET /product/{product_id} - получение продукта по ID
GET /products/search - поиск продуктов с фильтрацией

# Задание 5.1
POST /login - вход с установкой cookie
GET /user - защищенный маршрут

# Задание 5.2
POST /login_signed - вход с подписанной cookie
GET /profile_signed - защищенный маршрут с проверкой подписи

# Задание 5.3
POST /login_dynamic - вход с динамической сессией
GET /profile_dynamic - защищенный маршрут с продлением сессии

# Задание 5.4
GET /headers - получение заголовков User-Agent и Accept-Language
GET /info - получение заголовков с дополнительным X-Server-Time
